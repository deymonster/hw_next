# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Базовый URL для доступа к системе (замените на IP-адрес вашего сервера)
NEXT_PUBLIC_BASE_URL=http://your-server-ip

# IP-адрес сервера (замените на IP-адрес вашего сервера)
NEXT_PUBLIC_SERVER_IP=your-server-ip

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_DB=monitoring_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Prometheus
PROMETHEUS_PORT=9090

# Node Exporter
NODE_EXPORTER_PORT=9100

# Telegram BOT Token (получите у @BotFather в Telegram)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id

ADMIN_TELEGRAM_CHAT_ID=your_admin_telegram_chat_id

# Учетные данные администратора
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_admin_password
ADMIN_EMAIL=your_admin_email@example.com

# WINDOWS EXPORTER
export HANDSHAKE_KEY=your_secret_key

# PROMETHEUS
PROMETHEUS_PROXY_URL=http://nginx-proxy:8080
PROMETHEUS_USE_SSL=False
PROMETHEUS_TARGETS_PATH='./prometheus/targets/windows_targets.json'
PROMETHEUS_USERNAME=admin
PROMETHEUS_AUTH_PASSWORD=your_prometheus_password

# NODE
NODE_ENV='production'

# SMTP Configuration (для отправки email-уведомлений)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@example.com
SMTP_PASSWORD=your_email_password
SMTP_FROM_EMAIL=your_email@example.com
SMTP_FROM_NAME="Your Monitoring System Name"

# Admin Email
ADMIN_EMAIL=your_admin_email@example.com

# Ключ шифрования (сгенерируйте случайную строку)
ENCRYPTION_KEY=your_random_encryption_key

# REDIS
REDIS_PASSWORD=your_redis_password
REDIS_HOST=redis
REDIS_PORT=6379

# Next AUTH
NEXTAUTH_SECRET=your_nextauth_secret
NEXTAUTH_URL=http://your-server-ip
NEXT_PUBLIC_URL=http://your-server-ip

# Storage
NEXT_PUBLIC_STORAGE_URL=http://nginx-storage:80